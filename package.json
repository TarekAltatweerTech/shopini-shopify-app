{
  "name": "shopini-express-shopify-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Shopini Express — embedded Shopify app (static SPA, talks to the Laravel backend)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.26.9"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "vite": "^6.0.7"
  }
}
